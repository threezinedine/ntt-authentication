{
	"name": "auth-service",
	"scripts": {
		"compile": "tsc --project tsconfig.build.json && tsc-alias -p tsconfig.build.json",
		"dev": "npm run compile && ts-node dist/main.js",
		"dev:watch": "nodemon --config nodemon.dev.json"
	},
	"dependencies": {
		"express": "^5.1.0"
	},
	"devDependencies": {
		"@types/express": "^5.0.3",
		"@types/jest": "^29.5.14",
		"@types/node": "^24.0.1",
		"jest": "^29.7.0",
		"nodemon": "^3.1.10",
		"ts-node": "^10.9.2",
		"tsc-alias": "^1.8.16",
		"typescript": "^5.8.3"
	}
}
